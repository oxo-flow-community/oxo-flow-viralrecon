# Environment for PICARD_COLLECTMULTIPLEMETRICS — pinned exactly to the
# upstream nf-core/picard/collectmultiplemetrics module.
name: picard
channels:
  - conda-forge
  - bioconda
dependencies:
  - bioconda::picard=3.4.0
