# Environment for python-only rules (COLLAPSE_PRIMERS, NEXTCLADE_CLADE_MQC
# inline script) — pinned exactly to the upstream local/collapse_primers module.
name: python
channels:
  - conda-forge
  - bioconda
dependencies:
  - conda-forge::python=3.13.2
