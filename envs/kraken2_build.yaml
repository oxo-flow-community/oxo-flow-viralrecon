# Environment for KRAKEN2_BUILD — pinned exactly to the upstream
# local kraken2/build module.
name: kraken2_build
channels:
  - conda-forge
  - bioconda
dependencies:
  - bioconda::kraken2=2.1.2
  - conda-forge::pigz=2.6
