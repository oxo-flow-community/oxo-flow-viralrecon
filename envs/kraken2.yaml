# Environment for KRAKEN2_KRAKEN2 — pinned exactly to the upstream
# nf-core/kraken2/kraken2 module.
name: kraken2
channels:
  - conda-forge
  - bioconda
dependencies:
  - bioconda::kraken2=2.1.6
  - coreutils=9.5
  - pigz=2.8
